class Thoughts {
  let this.feelings = [];
  function feeler(context) {
    var defaults = {
      n: 'north'
      e: 'east'
      s: 'south'
      w: 'west'
    };
    var context = extend(defaults, context);
    for (context) {
      this.feelings.push(warrior.feel(direction))
    }
  }
}
